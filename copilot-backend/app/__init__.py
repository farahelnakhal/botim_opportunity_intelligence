"""BOTIM Product Discovery Copilot — conversational backend (read-only).

Pure Python 3 standard library. Reuses the repository's existing engines as
the single sources of truth; the conversation layer contains no scoring,
assumption, brief, or impact logic of its own.
"""
